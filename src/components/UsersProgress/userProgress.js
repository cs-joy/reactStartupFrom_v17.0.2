import React from 'react';
import PageProgress from 'react-page-progress';

class ExampleProgress extends React.Component {
    render() {
        return (
            <div>
                <PageProgress color={'skyblue'} height={5} />
            </div>
        );
    }
}

export default ExampleProgress
